<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NavFragment.SettingsFragment">

    <Switch
        android:id="@+id/setting_notification_opt"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:padding="12dp"
        android:text="Notification"
        android:textColor="@color/colorPrimaryDark"
        android:textStyle="bold"
        android:track="@color/colorPrimary"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="UseSwitchCompatOrMaterialXml" />

    <com.google.android.material.switchmaterial.SwitchMaterial
        android:id="@+id/setting_dark_mode_opt"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:padding="12sp"
        android:text="Dark Mode"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="16sp"
        android:textStyle="bold"
        android:theme="?attr/switchStyle"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:thumbTint="@color/colorPrimary"
        app:trackTint="@color/defaultGrey"
        app:useMaterialThemeColors="false"
        tools:ignore="UseSwitchCompatOrMaterialXml" />

</androidx.constraintlayout.widget.ConstraintLayout>